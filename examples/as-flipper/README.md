# Flipper contract example with subscript library

Simple contract of flipper example

**Install dependencies:**

```
yarn install
```

**Build Wasm target**

```
yarn build
```

After build, you can find the target wasm file in ./build/flipper.wasm
